# WHERE DEPLOY
version: '2'
services:

  wherecron:
    hostname: wherecron
    image: sylviefiat/docker-alpine-cron
    volumes:
      - ./wherecron_data/crontabs:/etc/cron.d
      - ./wherecron_data/scripts:/scripts
    
